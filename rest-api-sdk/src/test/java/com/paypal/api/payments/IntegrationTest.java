package com.paypal.api.payments;

/**
 * A functional test that hits external APIs over the wire (usually PayPal sandbox environment). 
 * Depending on the external API status, this test may not show expected behavior.
 */
public interface IntegrationTest {

}
